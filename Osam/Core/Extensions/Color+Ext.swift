import SwiftUI

extension Color {
    static let osamBackground = Color(uiColor: UIColor(red: 0.11, green: 0.12, blue: 0.14, alpha: 1))
    static let osamSurface = Color(uiColor: UIColor(red: 0.15, green: 0.16, blue: 0.18, alpha: 1))
    static let osamAccent = Color(uiColor: UIColor(red: 0.35, green: 0.65, blue: 0.95, alpha: 1))
    static let osamGreen = Color(uiColor: UIColor(red: 0.40, green: 0.78, blue: 0.45, alpha: 1))
    static let osamRed = Color(uiColor: UIColor(red: 0.90, green: 0.35, blue: 0.40, alpha: 1))
    static let osamOrange = Color(uiColor: UIColor(red: 0.90, green: 0.60, blue: 0.30, alpha: 1))
    static let osamText = Color(uiColor: UIColor(red: 0.85, green: 0.85, blue: 0.85, alpha: 1))
    static let osamSecondary = Color(uiColor: UIColor(red: 0.55, green: 0.55, blue: 0.60, alpha: 1))
    static let osamGutter = Color(uiColor: UIColor(red: 0.13, green: 0.14, blue: 0.16, alpha: 1))
}
